<?php
 
  // Session
  // behave like variables which is accessible anywhere
  // Unlike Cookies sessions are stored on the server
  
  // Login Logout
  // header('Location:/dashboard.php') - redirect to other page
   
  // session_start()  - whenever we use Session need to call session_start()
  
  // Destroy Session
  // session_start()
  // session_destroy()
  // header('Location:/dashboard.php') - redirect to other page

?>
